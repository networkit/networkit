#ifndef PRIM_H_
#define PRIM_H_

#include <vector>
#include <tuple>
#include "../graph/Graph.h"
#include "../auxiliary/PrioQueue.h"

namespace NetworKit {
class Prim {
	public:
	Prim() {}
	~Prim() {}
	
	std::vector<std::pair<node, node> > run(Graph& g, node v);
	
	bool test(Graph& g, std::vector<std::pair<node, node> > edges);
};

} /* namespace NetworKit */

#endif /* PRIM_H_ */

